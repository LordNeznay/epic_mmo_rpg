<fest:template xmlns:fest="http://fest.mail.ru">
    <fest:if test="json.back">
        <div align="center">
            <div class="button-wrapper back-button">
                <a href="#" class="button back-button__button">Back</a>
            </div>
        </div>
    </fest:if>
</fest:template>